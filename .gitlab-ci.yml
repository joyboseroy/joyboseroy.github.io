image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r *.html *.pdf *.ico *.jpg *.xml *.txt public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
